python -c "import json,pathlib; from IPython.core.inputtransformer2 import TransformerManager; n=json.loads(pathlib.Path('colab/minimind_colab_learning.ipynb').read_text()); code=[c for c in n['cells'] if c['cell_type']=='code']; tm=TransformerManager(); [compile(tm.transform_cell(''.join(c['source'])), f'<cell-{i}>', 'exec') for i,c in enumerate(code)]; assert all(c.get('execution_count') is None and not c.get('outputs') for c in code); setup=''.join(n['cells'][2]['source']); assert setup.startswith('%%bash\nset -euo pipefail\n') and max(map(len,setup.splitlines())) <= 100; print(f'notebook_schema=ok code_cells={len(code)} ipython_syntax=ok outputs_clean=ok multiline_bash=ok max_line={max(map(len,setup.splitlines()))}')" > wzh-steps/files/wzh-steps-2026-07-12-20-36/notebook-validation.txt 2>&1; rc=$?; printf 'exit_code=%s\n' "$rc" >> wzh-steps/files/wzh-steps-2026-07-12-20-36/notebook-validation.txt; cat wzh-steps/files/wzh-steps-2026-07-12-20-36/notebook-validation.txt; exit "$rc"
